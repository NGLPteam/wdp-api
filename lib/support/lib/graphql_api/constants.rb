# frozen_string_literal: true

module Support
  module GraphQLAPI
    module Constants
      # The default perPage size when not otherwise specified
      DEFAULT_PER_PAGE_SIZE = 25

      # The maximum amount to allow per page (used in several places)
      MAX_PER_PAGE_SIZE = 200
    end
  end
end
